{
  "name": "@diffy/backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cargo build",
    "dev": "portless api.diffy cargo watch --why -w src -w build.rs -w Cargo.toml -w ../../packages/rpc -x run",
    "lint": "cargo clippy -- -D warnings",
    "typecheck": "cargo check"
  },
  "devDependencies": {}
}
